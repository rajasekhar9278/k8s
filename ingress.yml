---
apiVersion: networking.k8s.io/v1	
kind: Ingress
metadata:
  annotations: 
    key: value
  labels: 
    environment: dev
    app: nginx
    name: nginx
  namespace: dev  
  
                  
                
  